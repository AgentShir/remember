import Typography from "typography";
import sternGroveTheme from "typography-theme-stern-grove";

sternGroveTheme.bodyWeigt = 500;
const typography = new Typography(sternGroveTheme);

export default typography;
